# Decisions

Append-only log of significant decisions and why they were made. Never edit or delete past entries — if a decision changes, add a new entry that supersedes it and says so. This is what makes "how did we get here" reconstructible later.

---

### 2026-08-23 — Split deployment: FastAPI backend on Railway, Next.js frontend on Vercel
**Decision:** Backend and frontend deploy to separate platforms rather than a single combined deploy.
**Why:** Railway handles the Python/FastAPI runtime and system-level dependencies (pymupdf needs system libs) well via `nixpacks.toml`; Vercel is the natural fit for Next.js. A single `vercel.json` covering both was tried and removed in favor of this split (see commits `4c9d873`, `9c79092`, `210541f`).

### 2026-08-23 — In-memory session storage for MVP
**Decision:** Uploaded file data and parsed results are held in a plain Python dict (`sessions`) in `main.py`, keyed by a generated session ID.
**Why:** Fastest way to get the upload → map → generate flow working end-to-end without standing up a database or Redis. Explicitly called out in code as MVP-only.
**Known consequence:** Won't survive a server restart, won't work across multiple backend instances, no persistence or encryption at rest. Real documents pass through this. Tracked in `docs/ROADMAP.md` as future work, not urgent yet.

### 2026-08-23 — LLM provider fallback chain: OpenAI → Gemini → fuzzy string matching
**Decision:** `services/llm_mapper.py` tries OpenAI first (if `OPENAI_API_KEY` set), falls back to Gemini (if `GEMINI_API_KEY` set) on failure or absence, and falls back further to plain fuzzy/substring matching if neither LLM is available or both fail.
**Why:** Avoids a hard dependency on a single LLM provider being up or configured — the app degrades gracefully to basic matching rather than failing outright. Currently only `OPENAI_API_KEY` is actually set in `.env`; the Gemini path exists but is unexercised in practice right now.

### 2026-08-23 — CORS allowlist explicitly scoped, not wildcard-open
**Decision:** `main.py` CORS middleware lists specific allowed origins (localhost dev URLs + the specific deployed Vercel domain) rather than allowing all origins.
**Why:** Fixed after initially missing the deployed frontend's actual domain (commit `b9643a3`, "Fix CORS: allow Vercel frontend domain"). Needs updating any time a new frontend deployment URL is added.

### 2026-08-23 — Documentation system established: docs/ + per-feature tickets
**Decision:** Added `docs/PROJECT.md`, `docs/ROADMAP.md`, `docs/DECISIONS.md`, `docs/TESTING.md`, and a `docs/tickets/` folder tracking feature work through `pending/ → ready-for-deploy/ → completed/` states, one markdown file per ticket.
**Why:** Nikhil wants every project session to be able to reconstruct project history and current state without re-deriving it from scratch, and wants each feature's request/plan/implementation/tests/impact documented as a permanent artifact rather than lost in chat history. `completed` requires the change to actually be deployed, not just tested — `ready-for-deploy` is the intermediate state for tested-but-not-yet-shipped work, since deployment is always confirmed with Nikhil explicitly rather than done autonomously.

### 2026-08-23 — Single `qa-reviewer` subagent instead of a multi-role team (architect/senior/junior/manager)
**Decision:** Added one custom subagent, `.claude/agents/qa-reviewer.md`, scoped to this project. It does an independent fresh-eyes review pass (correctness, test quality, data-handling risk, scope discipline) after implementation, as part of the "verify" step in the TDD workflow — it reports findings only, doesn't fix them.
**Why:** Nikhil considered setting up a full virtual org chart of subagents (architect, senior engineer, junior engineers, QA, manager) to drive TDD. Rejected: Claude Code subagents are stateless per invocation — a "senior engineer" agent has no memory of being that role last time, so a role hierarchy mostly adds coordination overhead and per-feature cost without adding real capability, since the underlying model is identical regardless of the label. What's genuinely useful is a reviewer with fresh eyes and no attachment to the implementation (a real, well-supported pattern — same idea as the `code-review` skill), so that's the one piece that got built.

### 2026-08-23 — Plan/review tooling routed by task size: custom agents for routine work, superpowers plan/subagent tooling for large decomposable work
**Decision:** For a normal single-coherent-change ticket, use the `planner` agent (global, `~/.claude/agents/planner.md`) for the plan step and `qa-reviewer` (project-local) for the verify step. Reserve the superpowers `writing-plans` skill and `subagent-driven-development` workflow (fresh subagent per task, each with an explicit `Interfaces: Consumes/Produces` contract, executed in isolated worktrees) for genuinely large, decomposable work with independent parallelizable pieces. Documented in `CLAUDE.md` under the feature/bugfix workflow.
**Why:** Compared both pairs directly. `planner` already writes straight into `docs/tickets/pending/NNN-slug.md` in the ticket template's own `Step → verify` format — it was built to match this project's conventions. `qa-reviewer` is pre-loaded with Dindu-specific risk knowledge (real financial documents, silent-wrong-mapping as the worst failure mode, in-memory session constraints, exact test commands) that a generic reviewer would need re-explained on every dispatch. `writing-plans` is a heavier apparatus by design — file-structure decomposition and inter-task contracts meant to let independent subagents implement tasks with zero shared context — and saves plans to `docs/superpowers/plans/`, a different location/format than `docs/tickets/`; defaulting to it on routine tickets would fragment the ticket system rather than add value. The superpowers tooling earns its overhead only when a task is actually large enough to split across parallel, non-overlapping subagent work (e.g. the eventual real frontend build).

### 2026-08-23 — `planner` dispatch made mandatory: Opus plans, Sonnet (session default) implements
**Decision:** The orchestrating session stays on the default model (Sonnet); the `planner` agent (pinned `model: opus` in its own frontmatter) must always be dispatched for the plan/research step on real code work, never skipped in favor of the orchestrating session doing that research and planning itself — even when the session has already read the relevant files and dispatching feels redundant.
**Why:** On ticket 001 (backend test suite), the orchestrating session explored the codebase and wrote the plan itself instead of dispatching `planner`, reasoning that it had already done equivalent research during brainstorming and a fresh dispatch would be redundant. Nikhil caught this: the point of `planner` isn't just producing a plan document, it's that planning/architecture happens at Opus tier specifically, while everything else (implementation, doc updates, git operations, routine conversation) runs on the cheaper default model. Skipping the dispatch defeats that split regardless of whether the resulting plan would have looked identical. The session's own general guidance ("don't spawn agents unless asked, handle it inline — spawning re-derives context and is the expensive path") is what actually pushed toward skipping it; that default is correct for avoiding pointless dispatch overhead, but doesn't apply here since this dispatch isn't pointless — it's the entire mechanism for the model-tiering architecture. Considered running the whole orchestrating session on Opus instead (removing the extra dispatch hop entirely); rejected as less cost-efficient for no real effectiveness gain, since the bulk of a session's volume is conversation, file reads, test runs, and doc bookkeeping — none of which are reasoning-bottlenecked enough to need Opus, unlike the planning decision itself.

### 2026-08-23 — Muted slate/steel-blue palette replaces the indigo/violet/purple theme (TICKET-002)
**Decision:** The frontend's dark theme accent moved from a saturated 3-stop indigo→violet→purple gradient (`#6366f1`→`#8b5cf6`→`#a855f7`, with matching colored glow shadows) to a single muted steel-blue accent (`#3f6d8f`, hue ~205° vs the old ~240–275°, roughly a third the saturation), backgrounds shifted from blue-black to neutral graphite, and colored box-shadow "glows" were replaced with plain neutral elevation shadows. The semantic success green (`#22c55e`) was muted to `#4a9a70` and split into a separate fill token (`--success`) and text token (`--success-text`, `#85c9a2`) in the same pass.
**Why:** Nikhil disliked the existing purple/indigo look (screenshot of the deployed Vercel app) and asked for something "a bit sophisticated." Offered three directions; he chose "keep the dark theme, swap the gradient for a restrained slate/graphite palette with one subdued accent — reads as enterprise SaaS, not AI demo." Bundled in a genuine, independently-discovered accessibility fix: white text on the old `#22c55e` was only ~2.4:1 contrast (WCAG AA requires 4.5:1) — confirmed with Nikhil before folding it into the same ticket rather than deferring it. All new text/background and label/fill pairs now verified ≥4.5:1 via an automated contrast check (see below).
**Also decided:** `frontend/src/app/page.module.css` was deleted — confirmed via `grep -rn "page.module" frontend/src` to have zero imports (untouched Next.js starter leftover defining a conflicting light-mode palette). Confirmed with Nikhil before deleting, per the project's autonomy preference around file deletion.

### 2026-08-23 — Interim `node:test` design-token harness, ahead of Vitest+RTL
**Decision:** For TICKET-002, added `frontend/tests/design-tokens.test.mjs` using Node's built-in `node:test` runner (`npm run test:tokens`) rather than bootstrapping Vitest + React Testing Library as part of the same ticket. It asserts old color literals are gone, new tokens match agreed hex/rgba values, and — the one part that's a real regression test rather than a lint rule — every text/background and label/fill color pair clears WCAG AA (4.5:1), computed via a contrast helper written directly in the test.
**Why:** There was no frontend test runner at all yet, and this change has no application logic to exercise — it's a pure design-token swap. `docs/TESTING.md` scopes the real Vitest+RTL setup to the first ticket that builds/changes actual component behavior; folding a test-infrastructure decision into a styling ticket would conflate two independent decisions and make both harder to review. The `test:tokens` script name deliberately avoids `test`, which stays reserved for Vitest. This harness doesn't replace Vitest+RTL and isn't meant to — it only covers design tokens, and the component-test-suite ticket (now overdue, since real components already exist) is unaffected.

### 2026-08-23 — Docs corrected: frontend was never "just the default starter template"
**Decision:** Corrected `docs/PROJECT.md`, `docs/ROADMAP.md`, `docs/TESTING.md`, and the project's own `CLAUDE.md`, all of which incorrectly stated the frontend was still the unbuilt default `create-next-app` template.
**Why:** Discovered while planning TICKET-002 — `frontend/src/app/page.tsx` is actually a real, complete 477-line single-page UI (upload → review mapping → generate flow) committed in the repo's very first commit ("Initial commit — DocFiller"), never reflected in the docs. It was apparently built directly rather than through the ticket process, so it predates and fell outside the `docs/tickets/` system entirely. Net effect: the "Frontend test suite" item on the roadmap is overdue, not merely planned — a real component now exists for Vitest+RTL to actually test, whereas the docs implied there was nothing yet to test.

### 2026-08-23 — Discovered and resolved a split Vercel deployment: two projects, only one in real use
**Decision:** While deploying TICKET-002's color change, discovered two separate Vercel projects both plausibly named for this app: `docfiller-app` (project ID `prj_3BELkIGBwHRLUkoAB2m2jIgYn4f8`, no GitHub integration, deployed only via manual CLI/API calls) and `doc-filler` (project ID `prj_gz0GblMrKGJ6OcImAHIZTqemOpBk`, GitHub-connected, auto-deploys on push to `main`). Confirmed `docfiller-app` is the real one — it's the exact URL (`docfiller-app.vercel.app`) named in the backend's CORS allowlist (`backend/main.py`) and the one `frontend/.vercel/project.json` links to locally — while `doc-filler`'s domains are SSO-protected and it isn't in the CORS allowlist, meaning nobody could actually reach it. Pushing to `main` (this session's earlier git push) built `doc-filler` successfully but left the real live site, `docfiller-app`, still serving the old indigo/violet palette. Resolved for now with a one-time manual deploy directly to `docfiller-app` via the Vercel API (`deploy_to_vercel`), confirmed live by checking the served CSS bundle for the new accent color and absence of the old one.
**Decided going forward (Nikhil, when asked to choose):** `docfiller-app` stays the one permanent production project — not `doc-filler` — specifically because it's already the live URL, already in the CORS allowlist, and already receiving real traffic; switching to `doc-filler` instead would have meant reconfiguring CORS, turning off SSO protection, and abandoning the current live URL for no benefit. `docfiller-app` should be connected to GitHub so future pushes auto-deploy, and `doc-filler` should then be deleted to remove the ambiguity. Neither step could be completed via the available Vercel MCP tools: `create_git_project` explicitly refuses to reconnect an existing unlinked project (it only creates new projects or reuses ones already linked to the target repo), and no delete-project tool exists. Both are left as manual dashboard steps — tracked in `docs/ROADMAP.md` — for Nikhil to complete.
**Also confirmed and documented:** the exact live Railway backend project/service/environment IDs (previously unconfirmed, flagged as "TBD" in `docs/PROJECT.md`) — project `adaptable-achievement`, service `DocFiller`. Railway's GitHub integration was already correctly wired and required no fix; its deployment of this session's commit (`8db4260`) succeeded on the first try.

### 2026-08-23 — Vercel split resolved: docfiller-app connected to GitHub, doc-filler deleted
**Decision:** Nikhil completed the two manual dashboard steps from the entry above himself: connected `docfiller-app` (the real production project) to `nikhilmalkari8/DocFiller` on GitHub, and deleted the stray `doc-filler` project. Confirmed via the Vercel API: `doc-filler` no longer appears in the project list at all, and `docfiller-app`'s project record now shows `link: {type: "github", org: "nikhilmalkari8", repo: "DocFiller"}`.
**Not yet confirmed:** whether the auto-deploy actually fires end-to-end — no push to `main` has happened since the connection was made, so the only deployment on record for `docfiller-app` is still the earlier one-time manual API deploy. Also unconfirmed: whether the project's Root Directory setting is `frontend` (needed for a monorepo build to succeed via git integration), since that setting isn't readable through the currently available Vercel MCP tools. Both are worth checking the next time a frontend change actually ships via a normal push.

### 2026-08-23 — Auto-deploy confirmed triggering, but every build fails: Root Directory not set
**Decision/finding:** The very next push to `main` (a docs-only commit, `f19d73a`) confirmed the GitHub integration does trigger a real build on `docfiller-app` automatically — closing the "not yet confirmed" gap from the entry above. But that build failed: `errorCode: "missing_pages_app"`, `"Command \"npm run vercel-build\" exited with 1"`. Root cause: the project's Root Directory setting is not `frontend`, so Vercel tries to build the Next.js app from the repo root (which has no `app/` or `pages/` directory there — those live under `frontend/src/app/`). This is the DocFiller repo's actual layout (backend and frontend as sibling directories), so any git-triggered build will keep failing until Root Directory is corrected.
**Impact:** None on production traffic — Vercel never promotes a failed build to serve live traffic, so `docfiller-app.vercel.app` kept serving the last successful deployment (the manual one from earlier this session) throughout. The practical effect is narrower than it sounds: auto-deploy is "on" but currently a no-op — every push to `main` will trigger a build, that build will fail, and the live site will keep serving whatever was last manually or successfully deployed until Root Directory is fixed.
**Left as a manual step** (Project Settings → General → Root Directory → `frontend`) because no field to read or set a project's Root Directory exists in the currently available Vercel MCP tools — same category of gap as the earlier git-reconnect and project-deletion steps.

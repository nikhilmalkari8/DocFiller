# TICKET-XXX: <short title>

**Status:** Pending | Ready for Deploy | Completed
**Created:** YYYY-MM-DD
**Moved to ready-for-deploy:** YYYY-MM-DD
**Completed (deployed):** YYYY-MM-DD

## Request
What was actually asked, in Nikhil's own words or a close paraphrase — not a cleaned-up restatement that loses intent.

## Plan
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
```

## Implementation
What was actually built or changed. List files touched, briefly note anything that deviated from the plan and why.

## Tests
What tests were written, what they cover, and their status (passing/failing). If TDD was followed, note the test existed and failed before implementation.

## Impact
What this affects — other parts of the system, breaking changes, new dependencies, data/schema changes, follow-up tickets it creates or blocks.

---

**Lifecycle:** create this file in `docs/tickets/pending/` when work starts → move to `docs/tickets/ready-for-deploy/` once implemented and tests pass → move to `docs/tickets/completed/` only once actually deployed and confirmed live. Update `docs/ROADMAP.md` at each transition.
